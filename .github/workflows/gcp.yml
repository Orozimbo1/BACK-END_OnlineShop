name: OnlineShop backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Setup Gcloud Account
    runs-on: ubuntu-latest
    environment: hw-prod
    env:
      IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/site:latest
    steps:
      # Git checkout
      - name: Checkout
        uses: actions/checkout@v2

      # Login to GCP
      - uses: google-github-actions/setup-gcloud@v0.2.0
        with:
          service_account_key: ${{ secrets.GCP_CREDENTIALS }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # gcloud configure docker
      - name: Configure Docker
        run: gcloud auth configure-docker --quiet

      # build image app
      - name: Build Docker image
        run: docker build -t $IMAGE_NAME ./app

      # push image to registry
      - name: Push Docker image app
        run: docker push $IMAGE_NAME

      # deploy image app
      - name: Deploy Docker image
        run: gcloud run deploy site --image $IMAGE_NAME --region us-central1 --memory 128Mi --min-instances 0 --max-instances 1 --platform managed --set-env-vars PORT=5000 --port $PORT --allow-unauthenticated
        
        # build image db
      - name: Build Docker image
        run: docker build -t $IMAGE_NAME ./app

      # push image to registry
      - name: Push Docker image db
        run: docker push $IMAGE_NAME

      # deploy image db
      - name: Deploy Docker image
        run: gcloud run deploy site --image $IMAGE_NAME --region us-central1 --memory 128Mi --min-instances 0 --max-instances 1 --platform managed --port 5432:5432 -e PORTA=5432 --allow-unauthenticated
